# Cursor coding — your mounted repos + optional Slack thread pins.
# Allowlisted Slack users only (SLACK_OWNER / SLACK_ALLOWED / SLACK_ALLOW_ALL).

repos:
  - id: compliancetracker
    local_cwd: /repos/compliancetracker
    repo: Orenda-Project/compliancetracker
    base_ref: main
    required_checks:
      - backend-ci
      - frontend-ci
      - e2e
    aliases:
      - ct
      - compliancetracker
      - pefsis
      - compliance tracker

threads:
  - channel_id: C0AV0MUTCJW
    thread_ts: "1784541760.548649"
    local_cwd: /repos/compliancetracker
    repo: Orenda-Project/compliancetracker
    base_ref: main
    required_checks:
      - backend-ci
      - frontend-ci
      - e2e
    jira_key: MC20-19085
    label: PEFSIS add-teacher vanish / MC20-19085
